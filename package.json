{
  "name": "opencode-plugin-cc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test 'tests/**/*.test.mjs'",
    "test:isolated": "node --test 'tests/isolated/**/*.test.mjs'",
    "test:live": "OPENCODE_LIVE=1 node --test 'tests/live/**/*.test.mjs'"
  }
}
