# Captured opencode streams

These are verbatim event-stream captures from opencode 1.18.13, produced by
model `openrouter/openai/gpt-oss-20b:free` on 2026-08-04. The task capture is a
successful task stream; the review capture is the captured review-agent error
stream. They are repository test data and must never be hand-edited. Replace a
capture only with a new verbatim capture and update this note.
